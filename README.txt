A simple php endpoint for accumulating SendGrid's Event API messages into a
simple extendable mysql database.

Currently the tests.ini file cannot contain spaces, strings must be pre-url
encoded for now.
